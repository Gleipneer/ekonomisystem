version: '3.9'
services:
  backend:
    build: .
    container_name: household_backend
    environment:
      - DATABASE_URL=sqlite:///./database.db
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    restart: unless-stopped